<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Invoice #<%= invoice.id %> - FreshStore</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="container">
      <a class="back" href="/dashboard">← Back to Dashboard</a>
      <h1>Invoice #<%= invoice.id %></h1>
      <p><strong>Item:</strong> <%= invoice.item_name %></p>
      <p><strong>Price:</strong> $<%= invoice.price %></p>
      <p><strong>Owner user_id:</strong> <%= invoice.user_id %></p>
      <hr />
      <h3>Secret Note</h3>
      <pre class="secret"><%= invoice.secret_note %></pre>
    </div>
  </body>
</html>